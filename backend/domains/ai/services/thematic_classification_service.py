class ThematicClassificationService:
    """Classifies blocks / promise text against the industry catalog
    using cosine similarity over embeddings."""
    def classify_block(self, embedding): pass
    def classify_promise(self, promise_id: str, promise_text: str): pass
